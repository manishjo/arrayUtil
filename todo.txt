
need a array util which can perform various oprations in array.

version 1.0:-
 frist i need to make a function "create" which will create  array and wrape it with structure,
 next i need to create another function "resize" which will resize the array,

 version 1.1:-
 find the index position of a element in the array,
 use of memcmp function in the find index function,